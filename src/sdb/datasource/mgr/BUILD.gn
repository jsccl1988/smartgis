# Copyright (c) 2026 The Mogu Authors.
# All rights reserved.

import("//build/smartgis.gni")

# Compiled into //src/sdb:sdb (dll_stem = sdb).
# No deps on //src/sdb/map:gis or :sde_gdal groups (same DLL — avoid GN cycles).
source_set("sde_mgr_sources") {
  configs += [ "//build:legacy" ]
  defines = [
    "_USRDLL",
    "SDE_MGR_EXPORTS",
  ]
  sources = [ "datasourcemgr.cpp" ]
  include_dirs += [ "//src" ]
  deps = [
    "//src/algorithm/geo:geo",
    "//src/base:base",
    "//src/base:core",
    "//third_party:gdal",
  ]
}

group("sde_mgr") {
  public_deps = [ "//src/sdb:sdb" ]
}
